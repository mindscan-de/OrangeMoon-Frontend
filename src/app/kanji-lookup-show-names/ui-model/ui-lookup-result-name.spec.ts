import { UiLookupResultName } from './ui-lookup-result-name';

describe('UiLookupResultName', () => {
  it('should create an instance', () => {
    expect(new UiLookupResultName()).toBeTruthy();
  });
});
