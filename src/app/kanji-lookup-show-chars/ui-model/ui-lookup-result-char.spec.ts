import { UiLookupResultChar } from './ui-lookup-result-char';

describe('UiLookupResultChar', () => {
  it('should create an instance', () => {
    expect(new UiLookupResultChar()).toBeTruthy();
  });
});
