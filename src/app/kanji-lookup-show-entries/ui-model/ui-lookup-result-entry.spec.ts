import { UiLookupResultEntry } from './ui-lookup-result-entry';

describe('UiLookupResultEntry', () => {
  it('should create an instance', () => {
    expect(new UiLookupResultEntry()).toBeTruthy();
  });
});
