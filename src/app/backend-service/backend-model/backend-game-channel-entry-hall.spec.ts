import { BackendGameChannelEntryHall } from './backend-game-channel-entry-hall';

describe('BackendGameChannelEntryHall', () => {
  it('should create an instance', () => {
    expect(new BackendGameChannelEntryHall()).toBeTruthy();
  });
});
