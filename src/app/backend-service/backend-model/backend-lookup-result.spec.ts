import { BackendLookupResult } from './backend-lookup-result';

describe('BackendLookupResult', () => {
  it('should create an instance', () => {
    expect(new BackendLookupResult()).toBeTruthy();
  });
});
