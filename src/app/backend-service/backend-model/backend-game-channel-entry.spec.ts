import { BackendGameChannelEntry } from './backend-game-channel-entry';

describe('BackendGameChannelEntryHall', () => {
  it('should create an instance', () => {
    expect(new BackendGameChannelEntry()).toBeTruthy();
  });
});
